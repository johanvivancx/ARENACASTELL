DATABASE_URL=postgresql://arena_app:CAMBIAR_PASSWORD@127.0.0.1:5432/arena_castell
APP_ORIGIN=http://127.0.0.1:8765
COOKIE_SECURE=false
# Activa el envío solamente después de configurar Google y preparar las tablas.
SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM_NAME=ARENA CASTELL
# Al publicar, usa aquí la URL HTTPS real. Vacío usa APP_ORIGIN (prueba local).
PUBLIC_BASE_URL=
